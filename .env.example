# Copy this file to `.env` (which is git-ignored).
# econdatapy expects exactly:  client_id;client_secret  (semicolon-separated —
# see login.py in the econdatapy package). Without this it pops a Tk dialog.
ECONDATA_CREDENTIALS=your-client-id;your-client-secret
